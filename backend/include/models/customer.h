#pragma once

#include <string>

// Simple customer data structure
struct Customer {
    int id = 0;
    std::string firstName;
    std::string lastName;
    std::string email;
    std::string phone;
}; 